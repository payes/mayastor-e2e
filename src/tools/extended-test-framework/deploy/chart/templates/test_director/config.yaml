server:
  AppVersion: 1.0.0
  Mode: Development
  DefaultTestPlan: {{ .Values.plan }}

logger:
  ReportCaller: false
  # text | json
  Formatter: json
  # debug | info | warn | error
  Level: info
  # file | stdout
  Output: stdout
